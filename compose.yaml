version: '3.11'

services:
  fastapi-lessons:
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn "bigger_applications:app" --host 0.0.0.0 --port 3000
    ports:
      - "3000:3000"